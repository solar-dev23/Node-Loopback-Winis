'use strict';

class SpinToWin {
  constructor(userId) {
    this.userId = userId;
  }

  calculateSpin() {
    
  }

  spin(options) {
    // this.userId = options.accessToken.id;
  }
}

module.exports = SpinToWin;
